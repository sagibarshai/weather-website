import ClipLoader from "react-spinners/ClipLoader";

const Loader = () => {
  return <ClipLoader color={"white"} loading size={"12px"} aria-label="Loading Spinner" data-testid="loader" />;
};
export default Loader;
